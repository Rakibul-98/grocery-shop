import React from 'react';
import useTitle from '../../Title/useTitle';

const CheckOut = () => {
    useTitle("Check out");

    return (
        <div>
            
        </div>
    );
};

export default CheckOut;