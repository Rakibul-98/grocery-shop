import React from 'react';
import useTitle from '../../Title/useTitle';

const CheckOut = () => {
    useTitle("Check Out");

    return (
        <div>
            wjfwioew
        </div>
    );
};

export default CheckOut;